% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tracker-filter.R
\name{tracked_entity_filter}
\alias{tracked_entity_filter}
\alias{\%.teq\%}
\alias{\%.tne\%}
\alias{\%.tgt\%}
\alias{\%.tge\%}
\alias{\%.tlt\%}
\alias{\%.tle\%}
\alias{\%.tlike\%}
\alias{\%.tsw\%}
\alias{\%.tew\%}
\alias{\%.tin\%}
\title{Tracked Entity Attribute Filter}
\usage{
tracked_entity_filter(attribute, operator, values, call = caller_env())

attribute \%.teq\% values

attribute \%.tne\% values

attribute \%.tgt\% values

attribute \%.tge\% values

attribute \%.tlt\% values

attribute \%.tle\% values

attribute \%.tlike\% values

attribute \%.tsw\% values

attribute \%.tew\% values

attribute \%.tin\% values
}
\arguments{
\item{attribute}{The tracked entity attribute id to filter on.}

\item{operator}{The comparison operator to apply.}

\item{values}{The value(s) to compare against. Not required for \code{null}
and \code{!null}.}

\item{call}{The caller environment.}
}
\value{
A spliced list with \code{filter} in the format \code{attribute:operator:value},
suitable for passing straight into \code{\link[=get_tracked_entities]{get_tracked_entities()}}.
}
\description{
Formats a filter on a tracked entity attribute for the DHIS2 Tracker API's
\code{trackedEntities} endpoint, in the \code{attribute:operator:value} form it
expects. This is a separate, smaller operator set from \code{\link[=metadata_filter]{metadata_filter()}}
— the two are not interchangeable, and the \verb{in} operator uses a different
value-joining convention (semicolons, no brackets).
}
\details{
As documented for the DHIS2 Tracker API, the supported operators are:
\itemize{
\item \code{eq}    - \verb{\%.teq\%}  - Equality
\item \code{ge}    - \verb{\%.tge\%}  - Greater than or equal
\item \code{gt}    - \verb{\%.tgt\%}  - Greater than
\item \code{le}    - \verb{\%.tle\%}  - Less than or equal
\item \code{lt}    - \verb{\%.tlt\%}  - Less than
\item \code{ne}    - \verb{\%.tne\%}  - Inequality
\item \code{like}  - \verb{\%.tlike\%} - Match anywhere
\item \code{sw}    - \verb{\%.tsw\%}  - Starts with
\item \code{ew}    - \verb{\%.tew\%}  - Ends with
\item \verb{in}    - \verb{\%.tin\%}  - Match one or more values
\item \code{null}  - (no infix form; call \code{tracked_entity_filter(attr, 'null', NULL)}) - Attribute has no value
\item \code{!null} - (no infix form; call \code{tracked_entity_filter(attr, '!null', NULL)}) - Attribute has a value
}

The infix operators are shorthand for the equivalent \code{tracked_entity_filter()}
call, e.g. \code{w75KJ2mc4zz \%.teq\% 'John'} is equivalent to
\code{tracked_entity_filter('w75KJ2mc4zz', 'eq', 'John')}. \code{null}/\code{!null} have no
infix form since an infix operator needs a right-hand value.

This filter is only documented for the \code{trackedEntities} endpoint. DHIS2's
published Tracker API docs do not describe an equivalent way to filter
\code{events} or \code{enrollments}, so \code{\link[=get_events]{get_events()}} and \code{\link[=get_enrollments]{get_enrollments()}}
reject a \code{filter} argument outright rather than silently sending it as
unsupported query syntax.

\code{\link[=metadata_filter]{metadata_filter()}} and its infix operators (\verb{\%.eq\%}, \verb{\%.in\%}, etc.) are
for a different DHIS2 API and are not interchangeable with this function
— passing one to \code{\link[=get_tracked_entities]{get_tracked_entities()}} raises an error, since several
metadata operators (e.g. \code{ieq}, \code{token}, the anchored \code{like} variants) and
the \verb{in}/\verb{!in} value-joining convention (comma-bracketed vs semicolon)
don't match what the Tracker API expects.
}
\examples{

# Tracked entities where attribute w75KJ2mc4zz equals "John"
tracked_entity_filter('w75KJ2mc4zz', 'eq', 'John')

# Tracked entities where attribute w75KJ2mc4zz is one of several values
tracked_entity_filter('w75KJ2mc4zz', 'in', c('John', 'Jane'))

# Equivalent, using the infix operator
w75KJ2mc4zz \%.teq\% 'John'

}
\seealso{
Other tracker functions: 
\code{\link{get_enrollments}()},
\code{\link{get_events}()},
\code{\link{get_relationships}()},
\code{\link{get_tracked_entities}()}
}
\concept{tracker functions}
