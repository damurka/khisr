% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_geo_features.R
\name{get_geo_features}
\alias{get_geo_features}
\title{Get Organisation Unit Geographic Features}
\usage{
get_geo_features(
  org_units,
  ...,
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{org_units}{A vector of organisation unit ids, or a DHIS2 org-unit
keyword such as \code{'LEVEL-2'} or \code{'USER_ORGUNIT'}.}

\item{...}{Other query parameters supported by your DHIS2 instance's
\code{geoFeatures} endpoint.}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the API response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble with one row per organisation unit: \code{id}, \code{name}, \code{code},
\code{level}, \code{type} (point or polygon), \code{parent_id}, \code{parent_name},
\code{parent_graph}, \code{coordinates} (a GeoJSON-style coordinate string),
\code{has_coordinates_down}, and \code{has_coordinates_up}. \code{NULL} if none were
found.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_geo_features()} retrieves the geographic features (coordinates/
boundaries) of organisation units from the \code{geoFeatures} endpoint —
useful for mapping. Confirmed live against a public DHIS2 demo instance.
}
\details{
Confirmed live that \code{org_units} must be sent as a \verb{ou:}-prefixed
dimension value (e.g. \code{ou:LEVEL-2} or \verb{ou:<uid1>;<uid2>}) — a bare
comma/semicolon-separated list of ids, without the \verb{ou:} prefix, is
rejected. A \code{dimensions} field in the raw response (category-dimension
filters applied to the query; empty in every case tested) is dropped
before returning, since an empty list-column in every row otherwise
collapses the whole result to 0 rows.
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

get_geo_features(org_units = 'LEVEL-2')
\dontshow{\}) # examplesIf}
}
