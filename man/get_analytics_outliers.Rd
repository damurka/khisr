% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_analytics_outliers.R
\name{get_analytics_outliers}
\alias{get_analytics_outliers}
\title{Get Statistical Outliers from DHIS2 Analytics}
\usage{
get_analytics_outliers(
  data_elements = NULL,
  data_sets = NULL,
  org_units,
  start_date,
  end_date,
  algorithm = NULL,
  threshold = NULL,
  ...,
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{data_elements}{Optional. A vector of data element ids to check.
Provide this or \code{data_sets}.}

\item{data_sets}{Optional. A vector of data set ids to check. Provide
this or \code{data_elements}.}

\item{org_units}{A vector of organisation unit ids to scope the query to.}

\item{start_date, end_date}{ISO-8601 dates bounding the query.}

\item{algorithm}{Optional. The detection algorithm: \code{'Z_SCORE'} (default),
\code{'MOD_Z_SCORE'}, or \code{'MIN_MAX'}.}

\item{threshold}{Optional. The sensitivity threshold for the chosen
algorithm; higher values flag fewer outliers.}

\item{...}{Other query parameters supported by your DHIS2 instance's
\code{analytics/outlierDetection} endpoint (e.g. \code{maxResults}, \code{orderBy}).}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the API response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble of outlier values as returned by DHIS2 (typically
including the data element, org unit, period, value, and statistical
bounds that flagged it), or \code{NULL} if none were found.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_analytics_outliers()} retrieves data values flagged as statistical
outliers by DHIS2's \code{analytics/outlierDetection} endpoint — useful for
data-quality review.
}
\details{
Requires either \code{data_elements} or \code{data_sets}. \strong{Unlike the rest of this
package, this endpoint's response shape has not been independently
verified live}: the public demo instance used to verify khisr's other
Tracker/analytics functions returned \verb{403 Forbidden} for this endpoint
regardless of parameters, which appears to be an account-authority
restriction rather than a request error. The field names below follow
DHIS2's published API documentation; confirm them against your own
instance before relying on this function in production.
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

\dontrun{
get_analytics_outliers(data_elements = 'lYsfXxCw6Qi',
                       org_units = 'W6sNfkJcXGC',
                       start_date = '2023-01-01',
                       end_date = '2023-12-31')
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_analytics]{get_analytics()}} for the underlying aggregated values.
}
