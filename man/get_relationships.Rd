% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_relationships.R
\name{get_relationships}
\alias{get_relationships}
\title{Get Relationships from a DHIS2 Instance}
\usage{
get_relationships(
  tracked_entity = NULL,
  enrollment = NULL,
  event = NULL,
  ...,
  fields = c("relationship", "relationshipType", "from", "to", "createdAt"),
  page_size = 500,
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{tracked_entity, enrollment, event}{Exactly one of these three must be
provided: a tracked entity, enrollment, or event id to retrieve
relationships for.}

\item{...}{Other query parameters supported by your DHIS2 instance's
Tracker API.}

\item{fields}{The DHIS2 field-selector for the columns to return.}

\item{page_size}{Number of records to request per page (default 500).}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the DHIS2 API
response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble of relationships, or \code{NULL} if none were found.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_relationships()} retrieves Tracker relationships — links between two
tracker objects (tracked entities, enrollments, or events), such as an
index case and a household contact — from DHIS2's Tracker API.
}
\details{
DHIS2 requires a relationships query to be scoped by exactly one of
\code{tracked_entity}, \code{enrollment}, or \code{event} — confirmed live against a
public DHIS2 demo instance, where omitting all three returns the exact
error \code{"Missing required parameter 'trackedEntity', 'enrollment' or 'event'."}. Pagination (nested \code{pager}, same as \code{\link[=get_tracked_entities]{get_tracked_entities()}}/
\code{\link[=get_events]{get_events()}}/\code{\link[=get_enrollments]{get_enrollments()}}) was also confirmed live, but the demo
instance tested had no relationship data configured on any program, so
the shape of a populated relationship's \code{from}/\code{to} fields is not
independently verified here — inspect the result and adjust \code{fields} as
needed for your instance.
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

# Relationships for a specific tracked entity
get_relationships(tracked_entity = 'qkU5JI6SQcd')
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_tracked_entities]{get_tracked_entities()}}, \code{\link[=get_events]{get_events()}}, \code{\link[=get_enrollments]{get_enrollments()}}

Other tracker functions: 
\code{\link{get_enrollments}()},
\code{\link{get_events}()},
\code{\link{get_tracked_entities}()},
\code{\link{tracked_entity_filter}()}
}
\concept{tracker functions}
