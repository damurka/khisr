% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_tracked_entities.R
\name{get_tracked_entities}
\alias{get_tracked_entities}
\title{Get Tracked Entities from a DHIS2 Instance}
\usage{
get_tracked_entities(
  program = NULL,
  tracked_entity_type = NULL,
  tracked_entities = NULL,
  org_units = NULL,
  org_unit_mode = NULL,
  updated_after = NULL,
  updated_before = NULL,
  enrolled_after = NULL,
  enrolled_before = NULL,
  occurred_after = NULL,
  occurred_before = NULL,
  ...,
  fields = c("trackedEntity", "trackedEntityType", "orgUnit", "createdAt", "updatedAt",
    "inactive"),
  page_size = 500,
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{program}{Optional. A program id to scope the query to.}

\item{tracked_entity_type}{Optional. A tracked entity type id to scope the
query to.}

\item{tracked_entities}{Optional. A vector of specific tracked entity ids
to retrieve.}

\item{org_units}{Optional. A vector of organisation unit ids to scope the
query to.}

\item{org_unit_mode}{Optional. One of \code{"SELECTED"}, \code{"CHILDREN"},
\code{"DESCENDANTS"}, \code{"ACCESSIBLE"}, \code{"CAPTURE"}, \code{"ALL"}, controlling how
\code{org_units} is interpreted. DHIS2 defaults to \code{"ACCESSIBLE"} when
\code{org_units} is not provided, and \code{"SELECTED"} when it is.}

\item{updated_after, updated_before}{Optional. ISO-8601 date or datetime
strings bounding the tracked entity's last-updated timestamp.}

\item{enrolled_after, enrolled_before}{Optional. ISO-8601 date or datetime
strings bounding the tracked entity's enrollment date.}

\item{occurred_after, occurred_before}{Optional. ISO-8601 date or datetime
strings bounding the tracked entity's enrollment incident/occurred date.}

\item{...}{One or more \code{\link[=tracked_entity_filter]{tracked_entity_filter()}} attribute filters, and/or
other query parameters supported by your DHIS2 instance's Tracker API.}

\item{fields}{The DHIS2 field-selector for the columns to return.}

\item{page_size}{Number of records to request per page (default 500).}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the DHIS2 API
response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble of tracked entities, or \code{NULL} if none were found.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_tracked_entities()} retrieves tracked entity instances (e.g. patients,
clients) from the DHIS2 Tracker API.
}
\details{
DHIS2 requires a tracked entities query to be scoped by at least one of
\code{program}, \code{tracked_entity_type}, \code{tracked_entities}, or \code{org_units};
an unscoped query will be rejected by the server. Nested repeating data
(e.g. \code{attributes}, \code{enrollments}) is returned as a list-column — use
\code{\link[tidyr:unnest_wider]{tidyr::unnest_wider()}}/\code{\link[tidyr:unnest_longer]{tidyr::unnest_longer()}} to flatten it further.
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

# All tracked entities enrolled in a program at a given org unit
get_tracked_entities(program = 'IpHINAT79UW',
                     org_units = 'DiszpKrYNg8',
                     org_unit_mode = 'DESCENDANTS')

# Tracked entities whose attribute w75KJ2mc4zz equals "John"
get_tracked_entities(program = 'IpHINAT79UW',
                     org_units = 'DiszpKrYNg8',
                     tracked_entity_filter('w75KJ2mc4zz', 'eq', 'John'))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=tracked_entity_filter]{tracked_entity_filter()}} for filtering on attribute values.

Other tracker functions: 
\code{\link{get_enrollments}()},
\code{\link{get_events}()},
\code{\link{tracked_entity_filter}()}
}
\concept{tracker functions}
