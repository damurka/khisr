% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_event_analytics.R
\name{get_event_analytics}
\alias{get_event_analytics}
\title{Retrieves Aggregated Event Analytics Data from DHIS2}
\usage{
get_event_analytics(
  program,
  ...,
  return_type = c("uid", "name"),
  page_size = 1000,
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{program}{A program id. Used as a URL path segment, not a query
parameter.}

\item{...}{One or more \code{\link[=analytics_dimension]{analytics_dimension()}} parameters (e.g. \code{ou}/\code{pe}
dimensions via \verb{\%.d\%}/\verb{\%.f\%}), and/or other query parameters supported
by your DHIS2 instance's Tracker analytics query API.}

\item{return_type}{Optional. \code{'uid'} (default) or \code{'name'} for the
identifier scheme used in the response.}

\item{page_size}{Number of records to request per page (default 1000).}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the API response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble of aggregated event analytics data, or \code{NULL} if none
was retrieved.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_event_analytics()} retrieves aggregated, dimensional analytics over
Tracker events from DHIS2's \code{analytics/events/query/{program}} endpoint —
as opposed to \code{\link[=get_events]{get_events()}}, which returns raw individual event records
from the Tracker API. Use this when you want counts/aggregates across
dimensions (e.g. events by org unit and period), not the underlying
records themselves.
}
\details{
The response shares the same \code{headers}/\code{rows} shape as \code{\link[=get_analytics]{get_analytics()}},
confirmed live against a public DHIS2 demo instance. Pagination is
automatic, but confirmed live to work differently from every other
paginated function in this package: without an explicit page size, this
endpoint silently returns only the first 50 rows, with no error or
warning — a real risk if you call the underlying DHIS2 endpoint directly
instead of through this function.
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

# Events for a program, by org unit, over the last year
get_event_analytics(program = 'IpHINAT79UW',
                    ou \%.d\% 'USER_ORGUNIT',
                    pe \%.d\% 'LAST_YEAR')
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_events]{get_events()}} for raw individual event records,
\code{\link[=get_enrollment_analytics]{get_enrollment_analytics()}} for the enrollment equivalent,
\code{\link[=get_analytics]{get_analytics()}} for aggregate (non-Tracker) analytics.
}
