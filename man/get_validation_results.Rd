% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_validation_results.R
\name{get_validation_results}
\alias{get_validation_results}
\title{Get Validation Rule Results from DHIS2}
\usage{
get_validation_results(
  org_units,
  start_date,
  end_date,
  ...,
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{org_units}{A vector of organisation unit ids to scope the query to.}

\item{start_date, end_date}{ISO-8601 dates bounding the query.}

\item{...}{Other query parameters supported by your DHIS2 instance's
\code{validationResults} endpoint (e.g. \code{vrg} to scope to a validation rule
group).}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the API response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble of validation results (organisation unit, period,
validation rule, and the left/right side values that violated it), or
\code{NULL} if none were found.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_validation_results()} retrieves violated validation rules for an
organisation unit and period range from DHIS2's \code{validationResults}
endpoint — useful for data-quality review.
}
\details{
Confirmed live against a public DHIS2 demo instance that this endpoint
(\verb{/api/validationResults}, not \verb{/api/analytics/validationResults}, which
404s) accepts these parameters and returns a valid, empty result —
the instance tested had no stored validation violations for the org units
and periods tried, so the shape of a populated result is not
independently verified.
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

get_validation_results(org_units = 'IWp9dQGM0bS',
                       start_date = '2023-01-01',
                       end_date = '2023-12-31')
\dontshow{\}) # examplesIf}
}
