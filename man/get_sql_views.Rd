% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_sql_views.R
\name{get_sql_views}
\alias{get_sql_views}
\title{Get SQL Views Metadata from a DHIS2 Instance}
\usage{
get_sql_views(
  ...,
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{...}{\code{\link[=metadata_filter]{metadata_filter()}} parameters, or their infix-operator
shorthand, to filter the results, and/or other query parameters
supported by your DHIS2 instance's \code{sqlViews} endpoint.}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the API response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble of SQL views (\code{id}, \code{name}, \code{type} — \code{'VIEW'} for a
materialized view queryable directly, or \code{'QUERY'} for one that must be
executed first), or \code{NULL} if none were found. Confirmed live against a
public DHIS2 demo instance.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_sql_views()} lists the SQL views configured on a DHIS2 instance —
predefined, admin-authored SQL queries exposed as a data endpoint. Use
\code{\link[=get_sql_view_data]{get_sql_view_data()}} to retrieve the actual data for one of them.
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

get_sql_views()
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_sql_view_data]{get_sql_view_data()}} for retrieving a view's data.
}
