% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_enrollment_analytics.R
\name{get_enrollment_analytics}
\alias{get_enrollment_analytics}
\title{Retrieves Aggregated Enrollment Analytics Data from DHIS2}
\usage{
get_enrollment_analytics(
  program,
  ...,
  return_type = c("uid", "name"),
  page_size = 1000,
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{program}{A program id. Used as a URL path segment, not a query
parameter.}

\item{...}{One or more \code{\link[=analytics_dimension]{analytics_dimension()}} parameters (e.g. \code{ou}/\code{pe}
dimensions via \verb{\%.d\%}/\verb{\%.f\%}), and/or other query parameters supported
by your DHIS2 instance's Tracker analytics query API.}

\item{return_type}{Optional. \code{'uid'} (default) or \code{'name'} for the
identifier scheme used in the response.}

\item{page_size}{Number of records to request per page (default 1000).}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the API response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble of aggregated enrollment analytics data, or \code{NULL} if
none was retrieved.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_enrollment_analytics()} retrieves aggregated, dimensional analytics
over Tracker enrollments from DHIS2's
\code{analytics/enrollments/query/{program}} endpoint — as opposed to
\code{\link[=get_enrollments]{get_enrollments()}}, which returns raw individual enrollment records from
the Tracker API. Use this when you want counts/aggregates across
dimensions (e.g. enrollments by org unit and period), not the underlying
records themselves.
}
\details{
The response shares the same \code{headers}/\code{rows} shape as \code{\link[=get_analytics]{get_analytics()}}.
Pagination is automatic; see \code{\link[=get_event_analytics]{get_event_analytics()}}'s details for a note
on this endpoint family's pagination behaviour (a different, silently
truncating shape confirmed live against a public DHIS2 demo instance for
the \code{events} variant, which this function shares the same underlying
engine with).
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

# Enrollments for a program, by org unit, over the last 12 months
get_enrollment_analytics(program = 'PREnRHSp3be',
                         ou \%.d\% 'USER_ORGUNIT',
                         pe \%.d\% 'LAST_12_MONTHS')
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_enrollments]{get_enrollments()}} for raw individual enrollment records,
\code{\link[=get_event_analytics]{get_event_analytics()}} for the event equivalent, \code{\link[=get_analytics]{get_analytics()}} for
aggregate (non-Tracker) analytics.
}
