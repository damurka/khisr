% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_complete_data_set_registrations.R
\name{get_complete_data_set_registrations}
\alias{get_complete_data_set_registrations}
\title{Get Raw Data Set Completeness Registrations from a DHIS2 Instance}
\usage{
get_complete_data_set_registrations(
  data_sets,
  org_units,
  children = NULL,
  periods,
  ...,
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{data_sets}{A vector of data set ids to scope the query to.}

\item{org_units}{A vector of organisation unit ids to scope the query to.}

\item{children}{Optional. If \code{TRUE}, also includes registrations for the
descendants of \code{org_units}, not just \code{org_units} itself. DHIS2 defaults
to \code{FALSE} (exact org units only) when not set.}

\item{periods}{A vector of ISO period strings (e.g. \code{'202501'}).}

\item{...}{Other query parameters supported by your DHIS2 instance's
\code{completeDataSetRegistrations} endpoint.}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the API response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble of registrations (\code{period}, \code{dataSet},
\code{organisationUnit}, \code{attributeOptionCombo}, \code{date}, \code{storedBy},
\code{completed}), or \code{NULL} if none were found.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_complete_data_set_registrations()} retrieves the raw
completeness-registration records (who marked a data set complete, and
when) from DHIS2's \code{completeDataSetRegistrations} endpoint — the raw
counterpart to \code{\link[=get_data_sets_by_level]{get_data_sets_by_level()}}'s aggregated reporting-rate
view, the same way \code{\link[=get_data_value_sets]{get_data_value_sets()}} complements \code{\link[=get_analytics]{get_analytics()}}.
}
\details{
Like \code{dataValueSets}, this endpoint is not paginated — it returns the
full matching result set in a single response. Confirmed live against a
public DHIS2 demo instance.
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

get_complete_data_set_registrations(data_sets = 'VEM58nY22sO',
                                    org_units = 'W6sNfkJcXGC',
                                    children = TRUE,
                                    periods = '202301')
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_data_sets_by_level]{get_data_sets_by_level()}} for aggregated reporting-rate metrics,
\code{\link[=get_data_value_sets]{get_data_value_sets()}} for the raw data-value equivalent.
}
