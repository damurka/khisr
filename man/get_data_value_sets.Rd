% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_data_value_sets.R
\name{get_data_value_sets}
\alias{get_data_value_sets}
\title{Get Raw Data Values from a DHIS2 Instance}
\usage{
get_data_value_sets(
  data_elements = NULL,
  data_sets = NULL,
  data_element_groups = NULL,
  org_units = NULL,
  org_unit_group = NULL,
  children = NULL,
  periods = NULL,
  start_date = NULL,
  end_date = NULL,
  last_updated = NULL,
  id_scheme = NULL,
  ...,
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{data_elements}{Optional. A vector of data element ids to scope the
query to.}

\item{data_sets}{Optional. A vector of data set ids to scope the query to.}

\item{data_element_groups}{Optional. A vector of data element group ids
to scope the query to.}

\item{org_units}{Optional. A vector of organisation unit ids to scope the
query to.}

\item{org_unit_group}{Optional. An organisation unit group id to scope
the query to.}

\item{children}{Optional. If \code{TRUE}, also includes data for the
descendants of \code{org_units}, not just \code{org_units} itself. DHIS2 defaults
to \code{FALSE} (exact org units only) when not set.}

\item{periods}{Optional. A vector of ISO period strings (e.g. \code{'202501'}).
Required unless \code{start_date}/\code{end_date} are given instead.}

\item{start_date, end_date}{Optional. ISO-8601 dates bounding the query,
as an alternative to \code{periods}.}

\item{last_updated}{Optional. ISO-8601 date or datetime string; only
returns data values updated after this point.}

\item{id_scheme}{Optional. Remaps identifiers in the response (e.g.
\code{'code'}) instead of the default DHIS2 UIDs.}

\item{...}{Other query parameters supported by your DHIS2 instance's
\code{dataValueSets} endpoint.}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the DHIS2 API
response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble of data values (\code{dataElement}, \code{period}, \code{orgUnit},
\code{categoryOptionCombo}, \code{attributeOptionCombo}, \code{value}, \code{storedBy},
\code{created}, \code{lastUpdated}, \code{comment}, \code{followup}), or \code{NULL} if none were
found.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_data_value_sets()} retrieves individually entered aggregate data
values from DHIS2's \code{dataValueSets} endpoint — as opposed to
\code{\link[=get_analytics]{get_analytics()}}, which returns pre-aggregated, computed values from the
analytics tables. Useful for data-quality auditing, or when you need the
raw entered values (including who entered them and when) rather than an
aggregated view.
}
\details{
DHIS2 requires this query to be scoped by at least one of \code{data_elements},
\code{data_sets}, or \code{data_element_groups}; by at least one of \code{org_units} or
\code{org_unit_group}; and by either \code{periods} or \code{start_date}/\code{end_date}. An
unscoped query will be rejected by the server. Confirmed live against a
public DHIS2 demo instance: without \code{children = TRUE}, \code{org_units} matches
only that exact organisation unit, not its descendants — a query that
returned 0 values with \code{org_units} alone returned real data once
\code{children = TRUE} was added.

Unlike the metadata and Tracker endpoints, \code{dataValueSets} is not
paginated — it returns the full matching result set in a single response.
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

# Raw data values for a data set at an org unit and everything below it,
# for a single period
get_data_value_sets(data_sets = 'VEM58nY22sO',
                    org_units = 'W6sNfkJcXGC',
                    children = TRUE,
                    periods = '202401')
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_analytics]{get_analytics()}} for pre-aggregated analytics values,
\code{\link[=get_data_sets_by_level]{get_data_sets_by_level()}} for data set reporting-rate metrics.
}
