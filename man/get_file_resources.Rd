% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_file_resources.R
\name{get_file_resources}
\alias{get_file_resources}
\title{Get File Resources Metadata from a DHIS2 Instance}
\usage{
get_file_resources(
  ...,
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{...}{\code{\link[=metadata_filter]{metadata_filter()}} parameters, or their infix-operator
shorthand, to filter the results, and/or other query parameters
supported by your DHIS2 instance's \code{fileResources} endpoint.}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the API response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble of file resources (\code{id}, \code{name}, \code{contentType},
\code{contentLength}), or \code{NULL} if none were found. Confirmed live against
a public DHIS2 demo instance.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_file_resources()} lists the metadata of files stored in a DHIS2
instance — icons, and files/images attached to tracker attribute values
or data values — from the \code{fileResources} endpoint. This retrieves
metadata only (name, content type, size); it does not download file
contents.
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

get_file_resources(contentType \%.like\% 'image')
\dontshow{\}) # examplesIf}
}
