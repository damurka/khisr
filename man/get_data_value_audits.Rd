% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_data_value_audits.R
\name{get_data_value_audits}
\alias{get_data_value_audits}
\title{Get Data Value Change History from a DHIS2 Instance}
\usage{
get_data_value_audits(
  data_elements = NULL,
  org_units = NULL,
  periods = NULL,
  ...,
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{data_elements}{Optional. A vector of data element ids to scope the
query to.}

\item{org_units}{Optional. A vector of organisation unit ids to scope
the query to.}

\item{periods}{Optional. A vector of ISO period strings to scope the
query to.}

\item{...}{Other query parameters supported by your DHIS2 instance's
\code{audits/dataValue} endpoint (e.g. \code{categoryOptionCombo},
\code{attributeOptionCombo}).}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the API response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble of data value audit records (data element, period, org
unit, category/attribute option combos, value, modified date, audit
type), or \code{NULL} if none were found.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_data_value_audits()} retrieves the audit trail (who changed a data
value, when, and to what) from DHIS2's \code{audits/dataValue} endpoint.
}
\details{
Confirmed live against a public DHIS2 demo instance that this endpoint
accepts these parameters and returns a valid, empty result — the data
value tried had no recorded edit history, so the shape of a populated
result is not independently verified.
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

get_data_value_audits(data_elements = 'lYsfXxCw6Qi',
                      org_units = 'W6sNfkJcXGC',
                      periods = '202301')
\dontshow{\}) # examplesIf}
}
