% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_sql_views.R
\name{get_sql_view_data}
\alias{get_sql_view_data}
\title{Get Data from a DHIS2 SQL View}
\usage{
get_sql_view_data(
  sql_view,
  variables = NULL,
  ...,
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{sql_view}{A SQL view id.}

\item{variables}{Optional. A named character vector of variable
substitutions for a parameterised SQL view, e.g.
\code{c(orgUnit = 'ImspTQPwCqd')}.}

\item{...}{Other query parameters supported by your DHIS2 instance's
\code{sqlViews} data endpoint.}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the API response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble of the SQL view's result rows, or \code{NULL} if none were
retrieved.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_sql_view_data()} retrieves the data produced by a SQL view
identified by \code{\link[=get_sql_views]{get_sql_views()}} — a predefined, admin-authored SQL query
exposed as a data endpoint.
}
\details{
A view of type \code{'QUERY'} (see \code{\link[=get_sql_views]{get_sql_views()}}) is executed on demand;
a \code{'VIEW'} is queried directly. \strong{This function's response shape has not
been independently verified live}: every SQL view on the public demo
instance used to verify khisr's other functions returned \verb{403 Forbidden}
("not authorised") or \verb{409 Conflict} (referencing an analytics table this
particular demo hadn't built) for this account — both account/instance
issues rather than request errors. The shape below follows DHIS2's
documented \code{listGrid} structure (shared with a few other legacy DHIS2
endpoints), reusing the same \code{headers}/\code{rows} parsing as \code{\link[=get_analytics]{get_analytics()}};
confirm it against your own instance before relying on this function in
production.
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

\dontrun{
views <- get_sql_views()
get_sql_view_data(views$id[1])
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_sql_views]{get_sql_views()}} for listing available SQL views.
}
