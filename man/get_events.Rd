% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_events.R
\name{get_events}
\alias{get_events}
\title{Get Events from a DHIS2 Instance}
\usage{
get_events(
  program = NULL,
  program_stage = NULL,
  org_units = NULL,
  org_unit_mode = NULL,
  updated_after = NULL,
  updated_before = NULL,
  occurred_after = NULL,
  occurred_before = NULL,
  ...,
  fields = c("event", "program", "programStage", "orgUnit", "status", "occurredAt",
    "scheduledAt", "dataValues"),
  page_size = 500,
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{program}{Optional. A program id to scope the query to.}

\item{program_stage}{Optional. A program stage id to scope the query to.}

\item{org_units}{Optional. A vector of organisation unit ids to scope the
query to.}

\item{org_unit_mode}{Optional. One of \code{"SELECTED"}, \code{"CHILDREN"},
\code{"DESCENDANTS"}, \code{"ACCESSIBLE"}, \code{"CAPTURE"}, \code{"ALL"}, controlling how
\code{org_units} is interpreted. DHIS2 defaults to \code{"ACCESSIBLE"} when
\code{org_units} is not provided, and \code{"SELECTED"} when it is.}

\item{updated_after, updated_before}{Optional. ISO-8601 date or datetime
strings bounding the event's last-updated timestamp.}

\item{occurred_after, occurred_before}{Optional. ISO-8601 date or datetime
strings bounding the event's occurred date (sent as the unprefixed
\code{occurredAfter}/\code{occurredBefore} query params — the corresponding
\code{\link[=get_tracked_entities]{get_tracked_entities()}} arguments use \code{eventOccurredAfter}/
\code{eventOccurredBefore} instead, to disambiguate from a tracked entity's
other nested dates).}

\item{...}{Other query parameters supported by your DHIS2 instance's
Tracker API (e.g. \code{status}, \code{assignedUserMode}).}

\item{fields}{The DHIS2 field-selector for the columns to return.}

\item{page_size}{Number of records to request per page (default 500).}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the DHIS2 API
response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble of events, or \code{NULL} if none were found.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_events()} retrieves tracker program-stage events from the DHIS2
Tracker API.
}
\details{
DHIS2 requires an events query to be scoped by at least one of \code{program},
\code{program_stage}, or \code{org_units}; an unscoped query will be rejected by the
server.

The \code{occurredAfter}/\code{occurredBefore} query parameter names used here
(unprefixed, unlike the equivalent \code{\link[=get_tracked_entities]{get_tracked_entities()}} arguments)
are based on the best available documentation and usage examples rather
than a directly confirmed parameter table for this specific endpoint —
verify against your own instance if these filters don't behave as
expected.

DHIS2's published Tracker API documentation does not describe a supported
way to filter events by data element value (unlike \code{\link[=get_tracked_entities]{get_tracked_entities()}},
which supports attribute filtering via \code{\link[=tracked_entity_filter]{tracked_entity_filter()}}), so a
\code{filter} argument raises an error here rather than being silently sent as
an unsupported query parameter. Retrieve the broader event set and filter
client-side instead. \code{dataValues} is returned as a list-column — use
\code{\link[tidyr:unnest_wider]{tidyr::unnest_wider()}}/\code{\link[tidyr:unnest_longer]{tidyr::unnest_longer()}} to flatten it further.
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

# All events for a program stage at a given org unit
get_events(program_stage = 'A03MvHHogjR',
           org_units = 'DiszpKrYNg8',
           org_unit_mode = 'DESCENDANTS')
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_tracked_entities]{get_tracked_entities()}}, \code{\link[=get_enrollments]{get_enrollments()}}

Other tracker functions: 
\code{\link{get_enrollments}()},
\code{\link{get_tracked_entities}()},
\code{\link{tracked_entity_filter}()}
}
\concept{tracker functions}
