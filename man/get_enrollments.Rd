% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_enrollments.R
\name{get_enrollments}
\alias{get_enrollments}
\title{Get Enrollments from a DHIS2 Instance}
\usage{
get_enrollments(
  program = NULL,
  org_units = NULL,
  org_unit_mode = NULL,
  updated_after = NULL,
  updated_before = NULL,
  enrolled_after = NULL,
  enrolled_before = NULL,
  occurred_after = NULL,
  occurred_before = NULL,
  ...,
  fields = c("enrollment", "program", "trackedEntity", "orgUnit", "status", "enrolledAt",
    "occurredAt"),
  page_size = 500,
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{program}{Optional. A program id to scope the query to.}

\item{org_units}{Optional. A vector of organisation unit ids to scope the
query to.}

\item{org_unit_mode}{Optional. One of \code{"SELECTED"}, \code{"CHILDREN"},
\code{"DESCENDANTS"}, \code{"ACCESSIBLE"}, \code{"CAPTURE"}, \code{"ALL"}, controlling how
\code{org_units} is interpreted. DHIS2 defaults to \code{"ACCESSIBLE"} when
\code{org_units} is not provided, and \code{"SELECTED"} when it is.}

\item{updated_after, updated_before}{Optional. ISO-8601 date or datetime
strings bounding the enrollment's last-updated timestamp.}

\item{enrolled_after, enrolled_before}{Optional. ISO-8601 date or datetime
strings bounding the enrollment date (sent as the unprefixed
\code{enrolledAfter}/\code{enrolledBefore} query params — the corresponding
\code{\link[=get_tracked_entities]{get_tracked_entities()}} arguments use \code{enrollmentEnrolledAfter}/
\code{enrollmentEnrolledBefore} instead, to disambiguate from a tracked
entity's other nested dates).}

\item{occurred_after, occurred_before}{Optional. ISO-8601 date or datetime
strings bounding the enrollment's incident/occurred date (sent as the
unprefixed \code{occurredAfter}/\code{occurredBefore} query params — the
corresponding \code{\link[=get_tracked_entities]{get_tracked_entities()}} arguments use
\code{enrollmentOccurredAfter}/\code{enrollmentOccurredBefore} instead).}

\item{...}{Other query parameters supported by your DHIS2 instance's
Tracker API (e.g. \code{status}).}

\item{fields}{The DHIS2 field-selector for the columns to return.}

\item{page_size}{Number of records to request per page (default 500).}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the DHIS2 API
response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble of enrollments, or \code{NULL} if none were found.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_enrollments()} retrieves program enrollments from the DHIS2 Tracker
API.
}
\details{
DHIS2 requires an enrollments query to be scoped by at least one of
\code{program} or \code{org_units}; an unscoped query will be rejected by the
server. DHIS2's Tracker API does not document filter support for this
endpoint, so a \code{filter} argument raises an error rather than being
silently sent as an unsupported query parameter; use
\code{\link[=get_tracked_entities]{get_tracked_entities()}} with \code{\link[=tracked_entity_filter]{tracked_entity_filter()}} to filter by
attribute value instead.

The \code{enrolledAfter}/\code{enrolledBefore}/\code{occurredAfter}/\code{occurredBefore}
query parameter names used here (unprefixed, unlike the equivalent
\code{\link[=get_tracked_entities]{get_tracked_entities()}} arguments) were confirmed by testing live
against a public DHIS2 demo instance — \code{enrolledAfter} measurably
narrowed the result set, and \code{orgUnits}/\code{orgUnit} both work for this
endpoint (unlike \code{events}, which only accepts the singular form).
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

# All enrollments in a program at a given org unit
get_enrollments(program = 'PREnRHSp3be',
                org_units = 'IWp9dQGM0bS',
                org_unit_mode = 'DESCENDANTS')
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_tracked_entities]{get_tracked_entities()}}, \code{\link[=get_events]{get_events()}}

Other tracker functions: 
\code{\link{get_events}()},
\code{\link{get_relationships}()},
\code{\link{get_tracked_entities}()},
\code{\link{tracked_entity_filter}()}
}
\concept{tracker functions}
