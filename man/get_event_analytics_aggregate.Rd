% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_event_analytics_aggregate.R
\name{get_event_analytics_aggregate}
\alias{get_event_analytics_aggregate}
\title{Get Aggregated (Pivot-Style) Event Analytics from DHIS2}
\usage{
get_event_analytics_aggregate(
  program,
  stage = NULL,
  ...,
  return_type = c("uid", "name"),
  retry = 2,
  verbosity = 0,
  timeout = 60,
  auth = NULL,
  call = caller_env()
)
}
\arguments{
\item{program}{A program id. Used as a URL path segment, not a query
parameter.}

\item{stage}{Optional. A program stage id to scope the query to.}

\item{...}{One or more \code{\link[=analytics_dimension]{analytics_dimension()}} parameters (e.g. \code{ou}/\code{pe}
dimensions via \verb{\%.d\%}/\verb{\%.f\%}), and/or other query parameters supported
by your DHIS2 instance's Tracker analytics aggregate API.}

\item{return_type}{Optional. \code{'uid'} (default) or \code{'name'} for the
identifier scheme used in the response.}

\item{retry}{Number of times to retry the API call in case of failure
(defaults to 2).}

\item{verbosity}{Level of HTTP information to print during the call.}

\item{timeout}{Maximum number of seconds to wait for the API response.}

\item{auth}{Optional. The authentication object.}

\item{call}{The caller environment.}
}
\value{
A tibble of aggregated event totals, or \code{NULL} if none was
retrieved.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{get_event_analytics_aggregate()} retrieves totals over Tracker events
from DHIS2's \code{analytics/events/aggregate/{program}} endpoint. This is a
genuinely different endpoint from \code{\link[=get_event_analytics]{get_event_analytics()}}'s
\code{analytics/events/query/{program}}: \code{query} returns line-list style rows
(one row per matched dimension combination, event-count driven), while
\code{aggregate} returns pivot-table style totals, and supports a \code{stage}
parameter to scope to one program stage.
}
\details{
The response shares the same \code{headers}/\code{rows} shape as \code{\link[=get_analytics]{get_analytics()}}.
Unlike \code{\link[=get_event_analytics]{get_event_analytics()}}'s \code{query} endpoint, this endpoint is
confirmed live to be unpaginated (no \code{metaData.pager}, like
\verb{/api/analytics} itself) — it returns the full result set in one request.
}
\examples{
\dontshow{if (khis_has_cred()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}

get_event_analytics_aggregate(program = 'PREnRHSp3be',
                              stage = 'mj1stImcUCi',
                              ou \%.d\% 'USER_ORGUNIT',
                              pe \%.d\% 'LAST_12_MONTHS')
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_event_analytics]{get_event_analytics()}} for the line-list \code{query} equivalent,
\code{\link[=get_enrollment_analytics_aggregate]{get_enrollment_analytics_aggregate()}} for the enrollment equivalent.
}
